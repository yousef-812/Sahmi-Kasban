# Sahmi Kasban — Phase 4 Implementation Log

## المرحلة

**المرحلة 4: تطبيق Flutter الأساسي**  
**الفرع:** `agent/flutter-core-app`  
**Pull Request:** #11  
**تاريخ البدء:** 2026-07-25  
**الحالة:** قيد التحقق النهائي — رحلات المنتج والمنصات والاختبارات الأساسية مكتملة.

## الهدف

بناء تطبيق Flutter عربي يستهلك الـAPIs المنفذة في المراحل 1–3، مع الحفاظ على جميع العمليات الحساسة والمالية داخل السيرفر.

## قرارات البداية

- إنشاء التطبيق داخل `mobile/` لأن الريبو لم يكن يحتوي مشروع Flutter سابقًا.
- عدم استخدام code generation في الدفعة الأولى لتقليل التعقيد وتسريع التحقق.
- استخدام Riverpod لإدارة الحالة وGoRouter للتنقل.
- استخدام Dio للاتصال بالـBackend.
- تخزين Access وRefresh tokens في `flutter_secure_storage` بدل SharedPreferences.
- استخدام SharedPreferences فقط لحالة إكمال الـOnboarding.
- تمرير عنوان الـAPI بواسطة `--dart-define=API_BASE_URL=...` وعدم تثبيت عنوان إنتاج داخل الكود.
- تنفيذ تجديد تلقائي للـAccess Token عند `401` باستخدام Refresh Token دوار.
- تنفيذ Logout محلي حتى عند انقطاع الشبكة، حتى لا يبقى المستخدم محبوسًا داخل جلسة منتهية.

## الدفعة الأولى — تأسيس التطبيق

### التأسيس

- `mobile/pubspec.yaml`
- `mobile/analysis_options.yaml`
- نقطة تشغيل ProviderScope.
- Theme عربي Material 3.
- إعداد API base URL حسب بيئة البناء.
- CI مستقل لـFlutter يخزن الـSDK وPub Cache داخل Workspace الريبو.

### الشبكة والجلسة

- `TokenStore` آمن.
- `ApiClient` يضيف Bearer Token تلقائيًا.
- منع تكرار طلبات Refresh المتزامنة عبر Future مشتركة.
- تدوير Refresh Token وحفظ الزوج الجديد.
- إعادة الطلب الأصلي مرة واحدة فقط بعد التجديد.
- مسح التوكنات عند فشل التجديد.
- تحويل أخطاء Dio إلى `ApiException` موحدة.

### التدفقات والشاشات

- Splash.
- Onboarding من ثلاث صفحات.
- تسجيل حساب.
- تسجيل دخول.
- استعادة الجلسة المخزنة عند بدء التطبيق.
- Dashboard بأربع وجهات: الرئيسية، التحليل، المحفظة، وحسابي.
- تحميل الملف الشخصي والمحفظة ومقدمة أحدث تقرير.
- Refresh بالسحب.
- Logout.
- إخلاء مسؤولية ظاهر في الصفحة الرئيسية.

## الدفعة الثانية — تأكيد الحساب واستعادته

تم تنفيذ وربط المسارات التالية بالـBackend:

```text
POST /api/v1/auth/verify-email
POST /api/v1/auth/resend-verification
POST /api/v1/auth/forgot-password
POST /api/v1/auth/reset-password
```

### تأكيد البريد

- بعد التسجيل ينتقل المستخدم مباشرة إلى شاشة تأكيد البريد.
- شاشة التحقق تقبل رمز التأكيد الكامل.
- يمكن إعادة إرسال رسالة التأكيد بعد إدخال بريد صحيح.
- محاولة تسجيل الدخول قبل التأكيد تنقل المستخدم إلى شاشة التحقق عند استجابة `403`.
- لا تعرض الواجهة أي توكن سري أو تفاصيل تكشف وجود حساب آخر.

### استعادة كلمة المرور

- إضافة رابط «نسيت كلمة المرور؟» داخل شاشة الدخول.
- طلب الاستعادة يعرض ردًا عامًا متوافقًا مع سياسة عدم كشف وجود البريد.
- شاشة إعادة التعيين تقبل رمز الاستعادة وكلمة المرور الجديدة وتأكيدها.
- التحقق المحلي يطابق سياسة السيرفر.
- بعد النجاح يعود المستخدم لتسجيل الدخول بالكلمة الجديدة.

## الدفعة الثالثة — رحلات المنتج الأساسية

### الصور الرمزية والملف الشخصي

- تم إنشاء 12 صورة رمزية فعلية باستخدام أداة توليد الصور.
- تم فصلها إلى ملفات WebP مستقلة بدقة `128×128` وضغط مناسب للتطبيق.
- إجمالي الحجم يقارب `23 KB` فقط.
- المسار: `mobile/assets/avatars/`.
- ربط المجلد داخل `pubspec.yaml`.
- تحديث Backend لإرجاع امتداد `.webp` بدل المسار القديم `.png`.
- شاشة تعديل الاسم واختيار الصورة من شبكة تفاعلية.
- حفظ التعديل على السيرفر وتحديث Session State فورًا.

### المحفظة

- إضافة شاشة سجل العمليات.
- Pagination بمقدار 20 عملية في الصفحة.
- Refresh وإعادة المحاولة وتحميل المزيد.
- عرض الخصم والإضافة وحالة العملية ووقتها.

### تحليل سهم EGX

- البحث داخل Registry الأسهم الموجود على السيرفر.
- اختيار السهم من النتائج بدون استخدام Radio API قديم.
- تأكيد تكلفة 0.5 عملة قبل الطلب.
- تعطيل الزر أثناء الطلب لمنع الضغط المكرر.
- عرض هل التحليل Cached وهل وقع خصم جديد.
- تحديث رصيد المستخدم والمحفظة بعد النجاح.
- عرض Payload التحليل بصورة قابلة للقراءة والنسخ.
- يعتمد الأمان المالي على قاعدة السيرفر: الخصم بعد النجاح فقط، وإعادة التحليل المخزن بلا خصم.

### تقرير أفضل 10

- فتح شاشة التقرير من المقدمة المجانية.
- إخفاء الأسهم قبل الفتح.
- تأكيد تكلفة الفتح قبل الطلب.
- استدعاء Endpoint الفتح الذري.
- التعامل مع التقرير المفتوح سابقًا بلا خصم مكرر.
- تحديث المحفظة والمقدمة بعد النجاح.
- عرض ترتيب الأسهم والدرجة وPayload كل سهم وملخص السوق.

## الدفعة الرابعة — المنصات والهوية والاختبارات

### Android وiOS

- إنشاء Android وiOS projects بواسطة Flutter Stable.
- Android application ID: `com.sahmikasban.sahmi_kasban_mobile`.
- iOS bundle identifier مطابق لإعداد المشروع.
- اسم التطبيق الظاهر: «سهمي كسبان».
- عدم إضافة سماح عام بـCleartext HTTP في نسخة الإنتاج.

### الهوية البصرية

- إنشاء Launcher icon بخلفية خضراء ورسم بياني صاعد ذهبي وأعمدة بيضاء.
- توليد كل أحجام Android mipmap.
- توليد كل أحجام iOS AppIcon.
- توليد Native Splash لـAndroid وiOS بخلفية العلامة وشعار مركزي.
- استخدام نفس الهوية داخل Flutter Splash.
- حفظ الأصل عالي الدقة في `mobile/assets/branding/app_icon.png`.

### الاختبارات الجديدة

- اختبار دورة حفظ وقراءة ومسح Access/Refresh tokens.
- اختبار أن الطلبات العامة لا تقرأ أو تضيف Authorization header.
- اختبار مشاركة عملية Refresh واحدة بين طلبين متزامنين حصلا على `401`.
- اختبار تدوير Refresh Token وحفظ الزوج الجديد.
- اختبارات موديلات سجل المحفظة والتحليل المدفوع والتقرير الكامل.
- اختبار اكتمال Registry الصور الرمزية ومسارات WebP.
- Widget smoke test لعرض بيانات التحليل المنظمة.

## تحديث ROADMAP

تم تحديث `ROADMAP.md` إلى النسخة `1.1` ليعكس الحالة الفعلية:

- المراحل 0 و1 و2 و3 مكتملة ومدمجة.
- المرحلة 4 قيد التنفيذ حتى نجاح الفحص النهائي والدمج.
- Flutter application هو المكوّن الجاري تطويره.
- أزيلت العلامات القديمة التي كانت تعرض Backend وWallet Ledger والمسح اليومي كأعمال غير مكتملة.

## ملاحظات CI التي ظهرت وعولجت

- تم تثبيت ترتيب التنسيق الصحيح: `flutter pub get` ثم Dart المرفق بنفس Flutter SDK.
- عولج تعارض اسم `TextDirection` الصادر من مكتبة `intl` باستخدام import مقيد لـ`DateFormat`.
- استبدلت خصائص `RadioListTile` المهجورة ببطاقات اختيار متوافقة مع Flutter الحالي.
- أزيلت ملفات وWorkflows التشخيص والتوليد المؤقتة بعد دفع نتائجها.
- عاد Workflow الرئيسي إلى صلاحية `contents: read` فقط.

## نتائج التحقق السابقة

- Workflow `30163540791`: الدفعة التأسيسية ناجحة.
- Workflow `30163889986`: تدفقات الحساب ناجحة.
- Core وBackend وPostgreSQL وAlembic استمروا في النجاح أثناء تطوير الدفعات التالية.

## المتبقي قبل إغلاق المرحلة 4

- نجاح Workflow النهائي على commit عادي من حساب المستودع.
- معالجة أي اختلاف يظهر في توقيع Dio/Flutter الحالي داخل الاختبارات الجديدة.
- تحديث `ROADMAP.md` إلى أن المرحلة 4 مكتملة بعد نجاح الفحص.
- تحويل PR #11 إلى Ready ودمجه في `main`.

## بوابة الدمج

لن تُدمج المرحلة قبل نجاح:

- `dart format --set-exit-if-changed`.
- `flutter analyze`.
- `flutter test`.
- مراجعة عدم وجود أسرار أو عنوان إنتاج ثابت.
- اختبار تدفق الجلسة والتجديد والخروج.
- اختبار رحلة المستخدم من التسجيل حتى فتح تقرير أو تنفيذ تحليل.
- تحديث `ROADMAP.md` بحالة المرحلة النهائية.
