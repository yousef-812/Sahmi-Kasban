# Sahmi Kasban — Implementation Log — Phase 2

هذا الملف هو السجل الدائم لأعمال المرحلة الثانية الخاصة ببيانات السوق وربط محركات التحليل والخصم من المحفظة.

---

## 2026-07-25 — المرحلة 2: بيانات EGX وتحليل السهم

**الحالة:** مكتملة ومدمجة في `main` عبر Pull Request رقم 6، Commit `10ce8d237b8ea4dcdf52a2275a9be19158d091e0`.

## الهدف المنفذ

تم بناء مسار كامل يبدأ برمز سهم EGX، يجلب بيانات تاريخية موحدة، يشغّل محركات Sahmi Kasban، يضيف شرحًا، يحفظ النتيجة، ثم يخصم `50` نقطة = `0.50` عملة بعد النجاح فقط.

## القرارات الاقتصادية

- تكلفة التحليل الجديد `50` نقطة = `0.50` عملة.
- لا يتم الخصم قبل نجاح جلب البيانات وتشغيل المحركات وإنشاء التقرير.
- التحليل المطابق الموجود في الكاش يُعاد دون خصم أو تشغيل مكرر.
- فشل البيانات أو المحركات أو قاعدة البيانات أو الخصم لا يستهلك الرصيد.
- Flutter لا يملك مسارًا مباشرًا للخصم أو إضافة الرصيد.

## واجهة بيانات السوق

تم إنشاء حزمة `backend/app/market_data/` وتتضمن:

- `types.py`: العقود والأنواع والأخطاء المشتركة.
- `egx_symbols.py`: سجل الرموز وتوحيد صيغة EGX وYahoo.
- `provider.py`: Adapter فعلي لـ`yfinance` وتصميم fallback.
- `cache.py`: تخزين واسترجاع Snapshots البيانات.

محركات التحليل تستقبل بيانات موحدة ولا تتصل بأي مزود خارجي مباشرة.

## سجل رموز EGX

- القائمة المنقولة من EGX-Pilot كانت تحتوي 156 مدخلًا.
- تم اكتشاف تكرار `EAST` و`KIMA`.
- القائمة الحالية تحتوي 154 رمزًا فريدًا.
- تتم إزالة `.CA` وتوحيد الحروف قبل التحقق.
- الرمز غير المسجل يُرفض قبل استدعاء مزود البيانات.

**قيد إنتاجي:** القائمة الحالية Seed Registry للتطوير وليست Master List رسمية، ويجب مطابقتها مع مصدر رسمي أو تجاري مرخص قبل الإطلاق.

## مزود yfinance

تم تنفيذ `YFinanceMarketDataProvider` باستخدام `Ticker.history` داخل thread منفصل حتى لا يحجب event loop في FastAPI.

الإعدادات الافتراضية:

- فترة سنة واحدة.
- فاصل يومي.
- مهلة 20 ثانية.
- حد أدنى 200 شمعة.
- مدة كاش 30 دقيقة.

تنظيف البيانات يشمل:

- اشتراط أعمدة OHLCV.
- تحويل القيم إلى أرقام.
- إزالة الأسعار المفقودة أو غير الموجبة.
- التحقق من منطق High وLow.
- منع الحجم السالب.
- ترتيب الشموع وإزالة التكرارات.
- إنشاء بصمة SHA-256 للبيانات بعد التطبيع.

`yfinance` معتمد للتطوير والبحث فقط في المرحلة الحالية؛ مزود الإنتاج المرخص لم يُحسم بعد.

## Snapshot cache

تمت إضافة جدول `market_data_snapshots` لحفظ:

- السهم والمزود والفترة والفاصل.
- وقت البيانات والجلب والانتهاء.
- بصمة الشموع وعددها.
- Payload الشموع الموحدة.

تمت إضافة قيد فريد على:

`ticker + provider + interval + period`

عند إنشاء Snapshot متزامن لنفس الهوية، يعيد الطلب الخاسر قراءة Snapshot الفائز بدل إرجاع خطأ داخلي.

## كاش نتائج التحليل

مفتاح الكاش يعتمد على:

- رمز السهم.
- بصمة الأسعار.
- لغة الشرح.
- إصدار المحركات.
- رأس المال وإعدادات المخاطر.
- الحد الأدنى للتاريخ.

الكاش مشترك بين المستخدمين لأن نتيجة المحركات لنفس البيانات والإعدادات واحدة. أثبت الاختبار أن أول مستخدم فقط يدفع، بينما يحصل المستخدم الثاني على النتيجة المخزنة دون خصم.

## ربط محركات Sahmi Kasban

تم إنشاء `backend/app/services/stock_analysis.py` لربط:

- `AnalysisConfig`.
- `SahmiKasbanAnalyzer`.
- بيانات OHLCV الموحدة.
- PostgreSQL.
- Wallet Ledger.
- شرح AI.

التقرير المحفوظ يحتوي:

- إصدار المحرك.
- المزود والفترة والفاصل.
- وقت وبصمة البيانات.
- عدد الشموع.
- التقرير الرقمي الكامل.
- الشرح ومصدره.
- إخلاء المسؤولية.

## الذكاء الاصطناعي

- المحركات الرقمية هي مصدر النتيجة الأساسي.
- AI مسؤول عن الشرح فقط.
- فشل مزود AI المصنف كـ`AIProviderError` يستخدم شرحًا عربيًا ثابتًا.
- تسجل النتيجة `explanation_source=ai` أو `deterministic`.
- أخطاء البرمجة العامة لا تُخفى داخل fallback.

## المعاملة المالية

الترتيب الفعلي:

1. التحقق من الرمز.
2. قراءة Snapshot أو جلب بيانات جديدة.
3. البحث عن تحليل مطابق في الكاش.
4. التحقق من توفر 50 نقطة للتحليل الجديد.
5. تشغيل المحركات.
6. إنشاء الشرح.
7. إنشاء سجل التحليل.
8. خصم 50 نقطة من Wallet Ledger.
9. Commit واحد للـSnapshot والتحليل والخصم.

أي فشل قبل الـcommit ينفذ rollback، فلا يبقى Snapshot جديد أو تحليل جزئي أو خصم.

تمت حماية سباق إنشاء تحليل مطابق بقيد `cache_key` الفريد؛ الطلب الفائز يدفع، والطلب المتزامن الآخر يعيد النتيجة الفائزة دون خصم إضافي.

## مسارات API

### قائمة الرموز

`GET /api/v1/market/instruments`

يدعم البحث الجزئي وتحديد عدد النتائج ويعيد رمز Yahoo بصيغة `.CA`.

### تحليل سهم

`POST /api/v1/stocks/{ticker}/analysis`

يتطلب مستخدمًا مسجلًا ومؤكد البريد، ويقبل:

```json
{
  "language": "ar"
}
```

الاستجابة تحتوي معرف التحليل، حالة الكاش، قيمة الخصم، الرصيد المتبقي، تاريخ البيانات، والتقرير الكامل.

أكواد الأخطاء:

- `404`: رمز غير مدعوم.
- `402`: رصيد غير كافٍ.
- `503`: بيانات السوق غير متاحة.
- `422`: المحركات لم تنتج تحليلًا صالحًا.

## Migration

تمت إضافة:

`backend/alembic/versions/0003_market_data_snapshots.py`

وهي قابلة للتطبيق والرجوع وإعادة التطبيق فوق Migratons المرحلتين السابقتين.

## الاختبارات المضافة

تم اختبار:

- توحيد رموز EGX وYahoo.
- رفض الرموز غير المعروفة.
- تنظيف OHLCV واستبعاد الشموع غير المنطقية.
- ثبات القائمة على 154 رمزًا فريدًا.
- خصم 50 نقطة بعد نجاح تحليل جديد.
- إعادة نفس التحليل مجانًا من الكاش.
- عدم استدعاء المزود مرة ثانية عند وجود Snapshot صالح.
- عدم الحفظ أو الخصم عند الرصيد غير الكافي.
- عدم الخصم عند فشل مزود البيانات.
- استخدام شرح ثابت عند فشل AI.
- أول مستخدم يدفع والمستخدم الثاني يستفيد من الكاش مجانًا.
- rollback كامل عند فشل المحركات بعد جلب البيانات.
- وجود تحليل واحد وSnapshot واحد وLedger debit واحد عند إعادة الطلب.

## CI والملاحظات التي عولجت

ظهرت 6 مخالفات Ruff شكلية: ثلاثة import blocks وثلاثة أسطر طويلة. تم استخدام `ruff --fix --diff` لمعرفة الترتيب الدقيق، واتضح أن `sahmi_kasban` تُعامل كحزمة محلية بعد `app`.

نجح Run ID `30138133991` بعد إضافة اختبارات المعاملات المركزة.

نجح الفحص النهائي النظيف Run ID `30138287798` في جميع الوظائف:

- Core lint.
- Core tests.
- Backend lint.
- Backend tests.
- Alembic upgrade إلى Migration 0003.
- Alembic downgrade إلى قاعدة فارغة.
- Alembic upgrade مرة أخرى على PostgreSQL 16.

تمت إزالة صلاحية كتابة تعليقات Pull Request وخطوات التشخيص المؤقتة، وإعادة CI إلى `contents: read` فقط.

## الملفات الرئيسية المضافة أو المعدلة

- `backend/app/market_data/`
- `backend/app/models/market_data.py`
- `backend/app/services/stock_analysis.py`
- `backend/app/api/routes/market.py`
- `backend/app/schemas/market.py`
- `backend/alembic/versions/0003_market_data_snapshots.py`
- `backend/tests/test_market_data_provider.py`
- `backend/tests/test_market_analysis_api.py`
- `backend/tests/test_market_analysis_transactions.py`
- `backend/README.md`
- `docs/MARKET_DATA_STRATEGY.md`
- `backend/.env.example`
- `backend/pyproject.toml`
- `.github/workflows/ci.yml`
- `IMPLEMENTATION_LOG_PHASE_2.md`

## القيود المتبقية

- مزود البيانات الحالي ليس مزود إنتاج مرخصًا.
- سجل الرموز ليس قائمة رسمية محدثة آليًا.
- لا توجد بيانات لحظية في مسار المنتج الحالي.
- تقويم جلسات EGX والعطلات لم يُنفذ بعد.
- المسح الكامل وتقرير أفضل 10 لم يُنفذا بعد.
- مراقبة المزود داخل لوحة الإدارة لم تُنفذ بعد.

## الخطوة التالية

المرحلة 3: تقويم جلسات التداول، Job الساعة 5 مساءً بتوقيت القاهرة، تحليل جميع الأسهم المؤهلة، اختيار أفضل 10، حفظ تقرير الجلسة التالية، وفتح التقرير مقابل عملة واحدة دون خصم مكرر.