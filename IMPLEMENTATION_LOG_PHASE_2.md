# Sahmi Kasban — Implementation Log — Phase 2

هذا الملف يسجل بالتفصيل جميع أعمال المرحلة الثانية الخاصة ببيانات السوق وربط محركات التحليل والخصم من المحفظة. أُنشئ كسجل مستقل للمرحلة حتى لا تضيع تفاصيل التنفيذ أو نتائج الفحوصات.

---

## 2026-07-25 — المرحلة 2: بيانات EGX وتحليل السهم

**الحالة:** مكتملة برمجيًا عبر Pull Request رقم 6، بانتظار نجاح الفحص النهائي النظيف والدمج في `main`.

## الهدف

بناء مسار كامل يبدأ من رمز سهم EGX، يجلب بيانات تاريخية موحدة، يشغّل محركات Sahmi Kasban، يضيف شرحًا، يحفظ النتيجة، ثم يخصم `50` نقطة = `0.50` عملة بعد النجاح فقط.

## القرارات الاقتصادية

- تكلفة التحليل الجديد: `50` نقطة = `0.50` عملة.
- لا يتم الخصم قبل نجاح جلب البيانات وتشغيل المحركات وإنشاء النتيجة.
- التحليل المطابق الموجود في الكاش يُعاد دون خصم.
- فشل مزود البيانات أو المحركات أو الحفظ أو الخصم لا يستهلك الرصيد.
- لا يوجد مسار يسمح لتطبيق Flutter بتنفيذ الخصم بنفسه.

## واجهة بيانات السوق

تم إنشاء حزمة `backend/app/market_data/` وتتضمن:

- `types.py`: العقود والأنواع والأخطاء المشتركة.
- `egx_symbols.py`: سجل رموز EGX وتوحيد صيغة الرموز.
- `provider.py`: Adapter لمزود `yfinance` وتصميم fallback.
- `cache.py`: تخزين واسترجاع Snapshots الأسعار.

تم فصل مزود البيانات عن محركات التحليل؛ المحركات تستقبل DataFrame موحدًا ولا تعرف مصدر الأسعار الخارجي.

## سجل رموز EGX

- تم نقل القائمة من مشروع EGX-Pilot القديم.
- كانت القائمة القديمة تحتوي 156 مدخلًا.
- تم اكتشاف تكرار `EAST` و`KIMA`.
- أصبحت القائمة بعد إزالة التكرار 154 رمزًا فريدًا.
- تتم إزالة `.CA` وتوحيد الحروف قبل التحقق.
- تحويل الرمز إلى Yahoo يتم بصيغة `TICKER.CA`.
- الرموز غير الموجودة في السجل تُرفض قبل الاتصال بالمزود.

### قيد معروف

القائمة الحالية Seed Registry وليست Master List رسمية. قد تحتوي على رموز قديمة أو تفتقد إدراجات حديثة، ولذلك يجب مطابقتها مع مصدر رسمي أو تجاري مرخص قبل الإنتاج.

## مزود yfinance

تم تنفيذ `YFinanceMarketDataProvider` باستخدام `Ticker.history` داخل thread منفصل حتى لا يتم حظر event loop في FastAPI.

الإعدادات الافتراضية:

- الفترة: سنة واحدة.
- الفاصل: يومي.
- مهلة الطلب: 20 ثانية.
- الحد الأدنى: 200 شمعة.
- مدة Snapshot cache: 30 دقيقة.

قواعد تنظيف البيانات:

- اشتراط أعمدة OHLCV.
- تحويل القيم إلى أرقام.
- إزالة الصفوف ذات الأسعار المفقودة.
- رفض الأسعار الصفرية أو السالبة.
- التحقق من ترتيب High وLow داخل الشمعة.
- منع الحجم السالب.
- ترتيب الشموع زمنيًا.
- إزالة التكرارات الزمنية.
- حفظ بصمة SHA-256 لمجموعة الشموع بعد التطبيع.

### قرار الإنتاج

`yfinance` مزود تطوير وبحث فقط في المرحلة الحالية. يجب اعتماد مزود بيانات رسمي أو تجاري مرخص قبل الإطلاق العام، ومراجعة شروط التخزين وإعادة عرض الأسعار.

## Snapshot cache

تم إنشاء جدول `market_data_snapshots` ويخزن:

- رمز السهم.
- المزود.
- الفترة والفاصل.
- وقت آخر بيانات.
- وقت الجلب والانتهاء.
- بصمة الشموع.
- عدد الشموع.
- Payload البيانات الموحدة.

تمت إضافة Unique Constraint على:

`ticker + provider + interval + period`

عند حدوث إنشاء متزامن لنفس Snapshot:

- يفوز أول طلب بإنشاء السجل.
- الطلب الآخر ينفذ rollback لعملية الإدخال المتعارضة.
- يعيد قراءة Snapshot الفائز ويكمل بدل إرجاع خطأ داخلي.

## كاش نتائج التحليل

مفتاح كاش التحليل يعتمد على:

- رمز السهم.
- بصمة بيانات الأسعار.
- لغة الشرح.
- إصدار المحركات.
- رأس المال الافتراضي.
- نسبة المخاطرة.
- الحد الأقصى لقيمة المركز.
- الحد الأدنى للتاريخ.

يؤدي تغير أي عنصر إلى إنشاء نتيجة جديدة. تطابق جميع العناصر يعيد التحليل المحفوظ دون تشغيل المحركات أو الخصم مرة أخرى.

الكاش مشترك بين المستخدمين لأن نتيجة المحركات لنفس البيانات والإعدادات واحدة. أثبت الاختبار أن أول مستخدم فقط يدفع، بينما يحصل المستخدم الثاني على نفس التحليل المخزن مجانًا.

## ربط محركات Sahmi Kasban

تم إنشاء `backend/app/services/stock_analysis.py` لربط:

- `AnalysisConfig`.
- `SahmiKasbanAnalyzer`.
- بيانات OHLCV الموحدة.
- قاعدة البيانات.
- Wallet Ledger.
- شرح الذكاء الاصطناعي.

تتضمن النتيجة المحفوظة:

- نسخة المحرك.
- المزود الفعلي.
- الفترة والفاصل.
- تاريخ البيانات.
- بصمة البيانات.
- عدد الشموع.
- التقرير الرقمي الكامل.
- الشرح.
- مصدر الشرح.
- إخلاء المسؤولية.

## الذكاء الاصطناعي والـfallback

- محركات التحليل الرقمية هي المصدر الأساسي للنتيجة.
- AI يستخدم لشرح النتيجة فقط.
- عند فشل مزود AI بخطأ `AIProviderError` يستخدم النظام شرحًا عربيًا ثابتًا.
- تسجل النتيجة `explanation_source=ai` أو `deterministic`.
- أخطاء البرمجة العامة لا يتم إخفاؤها باعتبارها فشل مزود AI.

## المعاملة المالية

الترتيب الفعلي للعملية:

1. التحقق من الرمز.
2. قراءة Snapshot صالح أو جلب بيانات جديدة.
3. البحث عن تحليل مطابق في الكاش.
4. التحقق من توفر 50 نقطة إذا لم يوجد كاش.
5. تشغيل المحركات.
6. إنشاء الشرح.
7. إنشاء سجل التحليل.
8. خصم 50 نقطة من Wallet Ledger.
9. Commit واحد للـSnapshot والتحليل والخصم.

إذا فشلت أي خطوة قبل الـcommit يتم rollback، فلا يبقى Snapshot جديد أو تحليل جزئي أو خصم.

تمت حماية سباق إنشاء تحليل مطابق بقيد `cache_key` الفريد. الطلب الفائز ينشئ التحليل ويدفع، والطلب المتزامن الآخر يعيد النتيجة الفائزة من الكاش دون خصم إضافي.

## مسارات API

### قائمة الرموز

`GET /api/v1/market/instruments`

يدعم:

- البحث الجزئي بالرمز.
- تحديد عدد النتائج من 1 إلى 200.
- إظهار رمز المزود بصيغة `.CA`.
- إظهار عدد رموز السجل الحالي.

### تحليل سهم

`POST /api/v1/stocks/{ticker}/analysis`

يتطلب مستخدمًا مسجلًا ومؤكد البريد.

Body:

```json
{
  "language": "ar"
}
```

الاستجابة تشمل:

- معرف التحليل.
- الرمز.
- هل النتيجة من الكاش.
- هل Snapshot الأسعار من الكاش.
- النقاط والعملات المخصومة.
- الرصيد المتبقي.
- وقت البيانات.
- Payload التقرير الكامل.

أكواد الأخطاء:

- `404`: الرمز غير مدعوم.
- `402`: الرصيد غير كافٍ.
- `503`: بيانات السوق غير متوفرة.
- `422`: المحركات لم تستطع إنتاج تحليل صالح.

## Migration

تمت إضافة Migration:

`backend/alembic/versions/0003_market_data_snapshots.py`

وتدعم:

- إنشاء الجدول والفهارس.
- الرجوع الكامل وحذف الجدول والفهارس.
- إعادة التطبيق فوق Migratons المرحلتين 0 و1.

## الاختبارات المضافة

### مزود البيانات

- توحيد رمز `COMI.CA` إلى `COMI`.
- تحويل الرمز إلى صيغة Yahoo.
- رفض رمز غير موجود.
- إزالة صف OHLC غير منطقي.
- تحويل الحجم السالب إلى صفر.
- إنتاج timestamps موحدة UTC.

### API والتحليل

- قائمة الرموز تحتوي 154 رمزًا فريدًا.
- التحليل الجديد يخصم 50 نقطة فقط.
- الرصيد يتغير من 300 إلى 250 نقطة بعد النجاح.
- إعادة نفس التحليل ترجع Cache دون خصم.
- المزود لا يُستدعى مرة ثانية عند وجود Snapshot صالح.
- يوجد تحليل واحد وSnapshot واحد وLedger debit واحد فقط.
- الرصيد غير الكافي يرجع 402 ولا يحفظ نتيجة أو Snapshot.
- فشل المزود يرجع 503 ويحافظ على 300 نقطة.
- فشل AI يستخدم الشرح الثابت ويكمل التحليل.
- الرمز غير المدعوم يُرفض قبل استدعاء المزود.
- مستخدمان لنفس التحليل: الأول يدفع والثاني يستخدم الكاش مجانًا.
- فشل المحركات بعد جلب 220 شمعة غير صالحة يرجع 422 ويعمل rollback لكل Snapshot وتحليل وخصم.

## ملاحظات CI

ظهرت في البداية 6 مخالفات Ruff شكلية:

- ثلاثة import blocks.
- ثلاثة أسطر أطول من الحد.

تم إصلاح الأسطر الطويلة، ثم استخدام وضع `ruff --fix --diff` لاستخراج ترتيب imports المطلوب. اتضح أن Ruff يصنف حزمة `sahmi_kasban` كحزمة محلية توضع بعد حزمة `app` داخل import block نفسه.

نجح Workflow Run ID `30138133991` في:

- Core lint.
- Core tests.
- Backend lint.
- Backend tests.
- Alembic upgrade إلى Migration 0003.
- Alembic downgrade إلى قاعدة فارغة.
- Alembic upgrade مرة أخرى على PostgreSQL 16.

تمت إزالة صلاحية `pull-requests: write` وخطوات التعليقات التشخيصية، وإعادة CI إلى وضعه النظيف بصلاحية قراءة المحتوى فقط.

## الملفات الرئيسية

- `backend/app/market_data/types.py`
- `backend/app/market_data/egx_symbols.py`
- `backend/app/market_data/provider.py`
- `backend/app/market_data/cache.py`
- `backend/app/models/market_data.py`
- `backend/app/services/stock_analysis.py`
- `backend/app/api/routes/market.py`
- `backend/app/schemas/market.py`
- `backend/alembic/versions/0003_market_data_snapshots.py`
- `backend/tests/test_market_data_provider.py`
- `backend/tests/test_market_analysis_api.py`
- `backend/tests/test_market_analysis_transactions.py`
- `backend/README.md`
- `docs/MARKET_DATA_STRATEGY.md`
- `backend/.env.example`
- `backend/pyproject.toml`
- `.github/workflows/ci.yml`
- `IMPLEMENTATION_LOG_PHASE_2.md`

## القيود المتبقية

- مصدر البيانات الحالي ليس مزود إنتاج مرخصًا.
- سجل الرموز ليس قائمة رسمية محدثة آليًا.
- لا توجد بيانات لحظية أو intraday في مسار المنتج الحالي.
- لم يتم تنفيذ تقويم جلسات EGX والعطلات بعد.
- لم يتم تنفيذ المسح الكامل لجميع الأسهم أو تقرير أفضل 10 بعد.
- لم تتم إضافة مراقبة مزود البيانات وقياس زمن الجلب في لوحة إدارة بعد.

## الخطوة التالية

المرحلة 3: تقويم جلسات التداول، Job الساعة 5 مساءً بتوقيت القاهرة، تحليل جميع الأسهم المؤهلة، اختيار أفضل 10، حفظ تقرير الجلسة التالية، وفتح التقرير مقابل عملة واحدة دون خصم مكرر.