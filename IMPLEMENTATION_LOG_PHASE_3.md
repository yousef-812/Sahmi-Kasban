# Sahmi Kasban — Phase 3 Implementation Log

## المرحلة

**المرحلة 3: تقرير أفضل 10 أسهم للجلسة القادمة**

**الفرع:** `agent/daily-top10-scan`  
**Pull Request:** #9  
**تاريخ التنفيذ:** 2026-07-25  
**الحالة الحالية:** جميع الفحوصات النهائية ناجحة والمرحلة جاهزة للدمج في `main`.

---

## الهدف المعتمد

تنفيذ خط كامل على السيرفر يقوم بعد جلسة التداول بـ:

1. التأكد من أن اليوم جلسة EGX فعلية.
2. التأكد من الوصول إلى الساعة 5:00 مساءً بتوقيت القاهرة.
3. جلب بيانات جميع الرموز الموجودة في سجل EGX الحالي.
4. رفض البيانات القديمة أو غير المكتملة أو ضعيفة السيولة.
5. تشغيل محركات Sahmi Kasban.
6. ترتيب الفرص بطريقة حتمية لا يتحكم فيها AI.
7. اختيار أفضل 10 فقط.
8. إضافة شرح AI بعد الاختيار، مع fallback ثابت.
9. حفظ تقرير ثابت للجلسة التالية.
10. فتح التقرير للمستخدم مقابل 100 نقطة = عملة واحدة.
11. منع الخصم المكرر أو بيع تقرير جزئي.

---

## القرارات المعمارية

### 1. التقويم

تم إنشاء `EGXTradingCalendar` في:

```text
backend/app/market_calendar.py
```

القواعد:

- أيام التداول الافتراضية: الأحد إلى الخميس.
- الجمعة والسبت عطلة.
- العطلات الرسمية قابلة للتحديث من `EGX_HOLIDAYS` كتواريخ ISO مفصولة بفواصل.
- التقرير يستهدف **جلسة التداول التالية** وليس اليوم التقويمي التالي.
- استدعاء المهمة قبل الموعد أو في عطلة لا ينشئ تقريرًا.

### 2. بوابة صحة بيانات الإغلاق

قائمة العطلات وحدها لا تكفي لأن العطلات قد تتغير أو تتأخر البيانات. لذلك يجب أن يساوي تاريخ آخر شمعة يومية تاريخ جلسة المصدر نفسها.

لو لم تتطابق آخر شمعة:

```text
excluded_reason = stale_close
```

ولا يدخل السهم في التقرير.

### 3. التوازي

المسح يستخدم `asyncio.Semaphore` بحد افتراضي 4 طلبات متزامنة:

```text
DAILY_SCAN_MAX_CONCURRENCY=4
```

الهدف هو منع فتح عدد كبير من اتصالات TradingView في نفس اللحظة، مع إبقاء المسح أسرع من التنفيذ التسلسلي.

### 4. فلاتر السيولة

قبل تشغيل المحركات، يتم حساب آخر 20 جلسة:

- متوسط قيمة التداول التقديرية = الإغلاق × الحجم.
- نسبة الجلسات ذات الحجم الأكبر من صفر.

الإعدادات الافتراضية:

```text
DAILY_SCAN_MIN_AVERAGE_TURNOVER_EGP=1000000
DAILY_SCAN_MIN_NONZERO_VOLUME_RATIO=0.80
```

### 5. الترتيب

AI لا يحدد الأسهم ولا ترتيبها. الترتيب حتمي حسب:

1. اجتياز التأهيل.
2. `BUY` قبل `WATCH`.
3. الدرجة النهائية.
4. مستوى الثقة.
5. متوسط السيولة.

إشارات `AVOID` لا تدخل قائمة المرشحين.

### 6. تفسير AI

يتم استدعاء AI لأفضل 10 فقط بعد انتهاء الترتيب. هذا يقلل التكلفة ويمنع النموذج من التأثير على الاختيار.

عند فشل AI يتم حفظ شرح عربي حتمي، ويُسجل:

```text
explanation_source = deterministic
```

### 7. عدم نشر تقرير ناقص

لو نتج أقل من العدد المطلوب:

```text
DAILY_REPORT_SIZE=10
```

فإن:

- لا يُنشأ `MarketReport` قابل للبيع.
- لا تُحفظ عناصر جزئية.
- يُسجل `MarketScanRun` بحالة `failed` وتفاصيل الاستبعادات والإخفاقات.

### 8. المعاملات المالية

فتح التقرير يكلف:

```text
DAILY_REPORT_COST_POINTS=100
```

أي 1.00 عملة في الواجهة.

الخصم وسجل الفتح داخل معاملة واحدة، ويستخدمان:

```text
transaction_id = market-report:{user_id}:{report_id}
```

مع Unique Constraint على `(user_id, report_id)`، وبالتالي:

- أول فتح يخصم 100 نقطة.
- الفتح المتكرر يرجع التقرير دون خصم.
- فشل إنشاء سجل الفتح يرجع الخصم بـrollback.
- التقرير الناقص لا يُخصم له شيء.

---

## الملفات الجديدة

```text
backend/app/market_calendar.py
backend/app/models/market_reports.py
backend/app/services/daily_reports.py
backend/app/schemas/reports.py
backend/app/api/routes/reports.py
backend/app/jobs/generate_daily_top10.py
backend/alembic/versions/0004_daily_market_reports.py
backend/tests/test_market_calendar.py
backend/tests/test_daily_reports.py
backend/tests/test_market_reports_api.py
docs/DAILY_TOP10_PIPELINE.md
IMPLEMENTATION_LOG_PHASE_3.md
```

## الملفات المعدلة

```text
backend/app/core/config.py
backend/app/models/__init__.py
backend/app/api/router.py
backend/.env.example
backend/README.md
```

---

## نماذج قاعدة البيانات

### `market_scan_runs`

يحفظ:

- جلسة المصدر.
- الجلسة المستهدفة.
- الموعد المجدول.
- بداية ونهاية التشغيل.
- حالة التشغيل.
- عدد الرموز الكلي.
- عدد الرموز التي تم تحليلها.
- عدد المؤهلين.
- عدد حالات الفشل.
- أسباب الاستبعاد والفشل.
- معرف التقرير الناتج.

يوجد تقرير تشغيل واحد فقط لكل جلسة مصدر.

### `market_report_unlocks`

يحفظ:

- المستخدم.
- التقرير.
- معرف عملية المحفظة.
- وقت الفتح.

يوجد سجل واحد فقط لكل مستخدم وتقرير.

---

## مسارات API

### Preview مجاني

```text
GET /api/v1/market/reports/latest/preview
```

يعرض:

- معرف التقرير.
- جلسة المصدر والجلسة المستهدفة.
- وقت التوليد.
- ملخص السوق.
- عدد العناصر.
- تكلفة الفتح.
- هل المستخدم فتح التقرير سابقًا.

ولا يعرض أسماء الأسهم أو عناصر التقرير.

### فتح التقرير

```text
POST /api/v1/market/reports/{report_id}/unlock
```

يرجع:

- قيمة الخصم.
- الرصيد المتبقي.
- التقرير الكامل.

### قراءة التقرير بعد الفتح

```text
GET /api/v1/market/reports/{report_id}
```

قبل الفتح يرجع `402` دون تسريب العناصر.

---

## تشغيل المهمة

```bash
cd backend
python -m app.jobs.generate_daily_top10
```

المهمة ترجع JSON بحالة من الحالات:

```text
created
already_exists
skipped / before_scan_time
skipped / non_trading_session
skipped / already_running
```

مثال Cron:

```cron
0 17 * * 0-4 cd /app/backend && /app/.venv/bin/python -m app.jobs.generate_daily_top10
```

حتى مع Cron، تظل بوابات التقويم والوقت والشمعة الأخيرة مطبقة داخل الخدمة.

---

## الاختبارات المضافة

### التقويم

- الجمعة والسبت عطلة.
- العطلة المضافة تُتخطى.
- حساب الجلسة التالية.
- رفض التشغيل قبل الساعة 5.
- قبول التشغيل عند الساعة 5.
- رفض التشغيل في عطلة أسبوعية.

### توليد التقرير

- إنشاء تقرير واحد بعشرة مراكز مرتبة.
- اختيار أعلى المرشحين حتميًا.
- عدم إعادة جلب البيانات أو إنشاء تقرير ثانٍ لنفس الجلسة.
- حفظ تفسير AI للمرشحين المختارين.
- عدم إنشاء تقرير جزئي عند وجود أقل من 10 مرشحين.
- تسجيل فشل المسح وتفاصيله.

### المحفظة والفتح

- خصم 100 نقطة في أول فتح.
- عدم الخصم في الفتح الثاني.
- سجل Unlock واحد.
- Wallet Entry واحد.
- الرصيد غير الكافي لا ينشئ خصمًا أو Unlock.
- التقرير ذو 9 عناصر لا يُباع ولا يُخصم له رصيد.

### API

- Preview لا يحتوي `items`.
- التقرير الكامل مقفول قبل الدفع.
- التقرير متاح بعد الدفع.
- الفتح المتكرر مجاني.

### قاعدة البيانات

GitHub Actions شغّل بنجاح:

- PostgreSQL 16.
- Alembic upgrade إلى `0004_daily_market_reports`.
- Alembic downgrade إلى قاعدة فارغة.
- Alembic rebuild إلى آخر Migration.

---

## مشاكل ظهرت أثناء التنفيذ

### Ruff B904

ظهر خطأ واحد:

```text
backend/app/services/daily_reports.py: B904
```

السبب: إنشاء استثناء داخل `except IntegrityError` دون ربطه بالاستثناء الأصلي.

تم إصلاحه باستخدام:

```python
raise DailyReportGenerationError(...) from exc
```

ثم تمت إزالة وظائف التشخيص والإصلاح المؤقتة من GitHub Actions، وعاد ملف CI إلى شكله النظيف.

---

## نتيجة الفحص النهائي

Workflow run:

```text
30161876519
```

النتيجة:

- Repository lint: passed.
- Core compile and Ruff: passed.
- Backend compile and Ruff: passed.
- Core and Backend tests: passed.
- PostgreSQL initialization: passed.
- Alembic upgrade/downgrade/rebuild: passed.

---

## قيود يجب تذكرها قبل الإنتاج

1. سجل الرموز الحالي Seed Registry وليس Master List رسميًا.
2. يجب تحديث `EGX_HOLIDAYS` من المصدر الرسمي عند أي تغيير.
3. حد السيولة الحالي إعداد أولي يحتاج معايرة على بيانات فعلية.
4. يلزم لاحقًا إضافة مراقبة تشغيل وتنبيه عند ارتفاع نسبة فشل مزود البيانات.
5. يلزم الاحتفاظ بأداء التقارير الإيجابي والسلبي دون حذف انتقائي.
6. جدولة Cron موثقة، لكن منصة الاستضافة النهائية ستحدد Scheduler الفعلي.

---

## معيار اكتمال المرحلة

- [x] لا يُنشأ تقرير في عطلة أو قبل الموعد.
- [x] التقرير مخصص لجلسة التداول التالية.
- [x] آخر شمعة تخص جلسة المصدر.
- [x] تحليل وفلاتر لكل الرموز المؤهلة.
- [x] ترتيب واختيار أفضل 10.
- [x] حفظ تقرير ثابت وسجل تشغيل.
- [x] Preview دون كشف الأسهم.
- [x] فتح مقابل عملة واحدة.
- [x] لا يوجد خصم مكرر.
- [x] التقرير الفاشل أو الناقص لا يستهلك الرصيد.
- [x] اختبارات وMigration ناجحة.
- [x] آخر Workflow بعد إضافة هذا السجل وREADME.
- [ ] دمج Pull Request #9 في `main`.
