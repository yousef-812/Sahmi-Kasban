# Sahmi Kasban — Phase 6 Implementation Log

> المرحلة: 6 — المجتمع والمناقشات  
> الحالة: قيد التنفيذ على Draft PR  
> آخر تحديث: 2026-07-26  
> المستودع: `yousef-812/Sahmi-Kasban`  
> الفرع: `agent/phase-6-community-core`  
> Pull Request: `#15 — Build Phase 6 community core`  
> Base: `main`  
> آخر Head موثق وقت كتابة السجل: `d790acc9089a7d34a0e9f9e1644c4808de520be1`

---

## 1. نقطة البداية

تم دمج PR #14 الخاص بحماية سلوك التوزيع الأسبوعي للعملات بنجاح في `main` قبل بدء المرحلة 6.

أثناء إنشاء فرع المرحلة 6 تم إنشاء ملف تجريبي باسم `x` على `main` بطريق الخطأ، ثم تم حذفه فورًا في commit لاحق. لا يوجد أي ملف أو محتوى متبقٍ من هذا الخطأ داخل الشجرة الحالية.

تم إنشاء فرع مستقل للمرحلة:

```text
agent/phase-6-community-core
```

وفتح Draft PR:

```text
https://github.com/yousef-812/Sahmi-Kasban/pull/15
```

يظل PR مسودة ولا يتم دمجه حتى اكتمال واجهات Flutter، التوثيق النهائي، وجميع بوابات القبول.

---

## 2. الهدف الوظيفي للمرحلة

المرحلة 6 تنفذ دورة المجتمع كاملة على السيرفر:

1. المستخدم يكتب عنوان المناقشة والمحتوى ويختار السهم والفترة.
2. السيرفر يحجز `50` نقطة، أي `0.5` عملة، من الرصيد المتاح.
3. تمر المناقشة على قواعد ثابتة وRegex.
4. تمر المناقشة المقبولة مبدئيًا على مراجعة AI واستخراج توقع منظم.
5. عند القبول يتم نشر المناقشة وتأكيد الحجز كخصم نهائي.
6. عند الرفض يتم تحرير الحجز وإعادة الرصيد مرة واحدة فقط.
7. عند فشل مزود AI لا يتم النشر ولا يتم الخصم النهائي؛ تبقى المناقشة `pending_review` والحجز محفوظًا لإعادة المحاولة أو المراجعة اليدوية.
8. يمكن للمستخدمين الإبلاغ عن المناقشات وكتم المستخدمين.
9. يمكن للإدارة مراجعة الحالات المختلف عليها، إخفاء المحتوى، حظر الحسابات، ومعالجة الاستئنافات مع Audit Logs.

---

## 3. قاعدة البيانات والموديلات

### Migration `0006_community_core`

تمت إضافة حقول ومحددات إلى جدول `discussions`:

- `submission_key`
- `content_fingerprint`
- `wallet_hold_transaction_id`
- `rejection_code`
- `reviewed_at`
- `hidden_at`
- Unique constraint على `(user_id, submission_key)`
- Unique constraint على `(user_id, content_fingerprint)`
- Unique constraint على `wallet_hold_transaction_id`
- حالات المناقشة المسموحة:
  - `pending_review`
  - `published`
  - `rejected`
  - `hidden`
- الفترات المسموحة:
  - `next_session`
  - `week`
  - `month`

تم إنشاء الجداول التالية:

- `discussion_reports`
- `user_mutes`
- `discussion_moderation_events`
- `community_admin_events`

تم إضافة فهارس لصف المجتمع، سجل المستخدم، البلاغات، الكتم، وأحداث المراجعة والإدارة.

### Migration `0007_community_appeals`

تم إنشاء جدول `discussion_appeals` لدعم استئناف واحد فقط لكل مناقشة، ويشمل:

- حالة المناقشة وقت الاستئناف: `rejected` أو `hidden`
- رسالة الاستئناف
- الحالة: `open` أو `accepted` أو `rejected`
- بيانات القرار الإداري
- المسؤول الذي حسم الاستئناف
- `publish_transaction_id` فريد عند إعادة نشر مناقشة سبق رفضها

جميع migrations قابلة للتطبيق والتراجع وإعادة البناء عبر Alembic.

---

## 4. Wallet Hold ومعاملات الرصيد

تم توسيع خدمة المحفظة لإضافة دورة حجز آمنة:

- `hold_points`: يخصم النقاط من الرصيد المتاح ويسجل Ledger Entry بحالة حجز.
- `confirm_hold`: يحول الحجز إلى خصم مؤكد من دون خصم جديد.
- `release_hold`: يعيد النقاط ويسجل عملية تحرير مستقلة وفريدة.

قواعد المرحلة:

```text
DISCUSSION_COST_POINTS = 50
```

- القبول: تأكيد الحجز فقط.
- الرفض: تحرير الحجز وإعادة `50` نقطة.
- إعادة نفس الطلب بنفس `submission_key`: نتيجة idempotent من دون حجز جديد.
- إعادة نفس المحتوى بمفتاح مختلف: إرجاع المناقشة الأصلية idempotently من دون خصم أو حجز جديد.
- أي تعارض في transaction ID أو حالة الحجز يوقف العملية بدل إنشاء خصم مزدوج.

بالنسبة للاستئناف:

- قبول استئناف مناقشة `hidden`: يعيد نشرها من دون خصم جديد لأن الخصم الأصلي كان مؤكدًا.
- قبول استئناف مناقشة `rejected`: يخصم `50` نقطة عند النشر لأن الحجز الأصلي كان قد تم تحريره وإعادة الرصيد.
- عملية نشر الاستئناف لها transaction ID فريد وتعمل idempotently.

---

## 5. القواعد الثابتة وRegex

يتم فحص العنوان والمحتوى قبل AI. القواعد الحالية تمنع:

- الروابط الخارجية، بما فيها HTTP/HTTPS و`www` وبعض روابط التواصل.
- أرقام الهواتف المصرية.
- بيانات أو عبارات التواصل مثل واتساب وتليجرام والتواصل المباشر.
- الإعلانات والدعوة لتحميل تطبيق أو الاشتراك في قناة/مجموعة.
- ادعاءات الربح المضمون أو عدم وجود خسارة.
- بعض أنماط الإساءة المباشرة.

نتيجة القواعد تحفظ داخل `moderation_result.rules` مع:

- `passed`
- `reason_codes`
- نتيجة كل Check

إذا فشلت القواعد يتم رفض المناقشة فورًا وتحرير الحجز من دون استدعاء AI.

---

## 6. مراجعة AI واستخراج التوقع المجمّد

تم ربط Backend بخدمة `SahmiAIService` الموجودة في المشروع، والتي تدعم:

- Open-WebUI كمسار أساسي عند ضبطه.
- Groq مع تدوير المفاتيح كـfallback.

تدفق المراجعة:

1. تحميل المناقشة `pending_review`.
2. التأكد أن القواعد الثابتة مرت بنجاح.
3. استدعاء `moderate_discussion`.
4. عند رفض AI يتم حفظ السبب وتحرير الحجز.
5. عند قبول المحتوى يتم استدعاء `extract_prediction`.
6. يتم تطبيع الاتجاه إلى:
   - `up`
   - `down`
   - `neutral`
7. إذا كان الاتجاه غير واضح يتم الرفض بسبب `prediction_not_clear` وإعادة الرصيد.
8. عند النجاح يتم إنشاء `frozen_prediction` يحتوي على:
   - الإصدار
   - السهم المختار من السيرفر
   - الاتجاه
   - الهدف السعري إن وجد
   - الفترة
   - الموعد النصي إن وجد
   - الادعاءات المستخرجة
   - درجة specificity
   - SHA-256 للنص الأصلي
   - وقت التجميد
9. يتم تأكيد الحجز ونشر المناقشة.

### فشل مزود AI

فشل Open-WebUI/Groq لا يؤدي إلى نشر تلقائي أو خسارة الرصيد:

- الحالة تظل `pending_review`.
- الحجز يظل محفوظًا.
- `review_stage` يصبح `awaiting_ai_retry`.
- يتم زيادة عدد المحاولات.
- يتم تسجيل `ai_failed` في سجل المراجعة من دون تخزين الأسرار أو تفاصيل حساسة.

---

## 7. منع السبام والتكرار

تمت إضافة طبقة `community_safety.py` وتشمل:

### بصمة المحتوى

يتم تطبيع Unicode والمسافات وحالة الأحرف، ثم حساب SHA-256 من:

- ticker
- title
- content
- period_type

البصمة فريدة لكل مستخدم. إرسال نفس المحتوى بمفتاح جديد يعيد النتيجة الأصلية ولا يحجز رصيدًا جديدًا.

### Rate limiting دائم في قاعدة البيانات

- أقصى حد: `3` محاولات إنشاء خلال `15` دقيقة.
- أقصى حد: `10` محاولات إنشاء خلال `24` ساعة.
- عند تجاوز الحد يرجع API حالة `429` مع Header باسم `Retry-After`.
- يتم استخدام قفل صف المحفظة لتسلسل طلبات المستخدم نفسه ومنع تجاوز العدادات بالطلبات المتزامنة على PostgreSQL.

---

## 8. واجهات API للمستخدم

المسارات الأساسية تحت `/api/v1`:

```text
POST   /community/discussions
GET    /community/discussions
GET    /community/discussions/mine
GET    /community/discussions/{discussion_id}
POST   /community/discussions/{discussion_id}/reports
PUT    /community/users/{user_id}/mute
DELETE /community/users/{user_id}/mute
POST   /community/discussions/{discussion_id}/appeals
GET    /community/appeals/mine
```

الخصائص المنفذة:

- إنشاء المناقشة ومراجعتها ونشرها أو رفضها.
- عرض Feed المنشور فقط.
- فلترة المجتمع حسب السهم.
- إخفاء المستخدمين المكتومين من Feed.
- صفحة مناقشاتي بجميع الحالات وسبب الرفض ونتيجة المراجعة.
- منع المستخدم من رؤية مناقشة غير منشورة لا يملكها.
- منع الإبلاغ عن مناقشة المستخدم نفسه.
- منع تكرار البلاغ نفسه من المستخدم نفسه.
- كتم وإلغاء كتم idempotently.
- إنشاء استئناف واحد فقط لكل مناقشة مرفوضة أو مخفية.
- عرض استئنافات المستخدم.

---

## 9. الإدارة والمراجعة اليدوية

تمت إضافة صلاحية Admin مبنية على متغير البيئة:

```text
ADMIN_EMAILS=email1@example.com,email2@example.com
```

المستخدم يجب أن يكون مسجلًا ونشطًا وبريده موجودًا في allowlist حتى يحصل على `CurrentAdmin`.

مسارات الإدارة:

```text
GET  /admin/community/discussions
POST /admin/community/discussions/{discussion_id}/action
POST /admin/community/users/{user_id}/block
POST /admin/community/users/{user_id}/unblock
GET  /admin/community/appeals
POST /admin/community/appeals/{appeal_id}/resolve
```

إجراءات المناقشة اليدوية:

- `approve`: يحتاج توقعًا منظمًا قبل النشر.
- `reject`: يحتاج reason code ويعيد الحجز للمستخدم.
- `hide`: يخفي مناقشة منشورة ويغلق بلاغاتها المفتوحة.
- `restore`: يعيد المناقشة المخفية إلى المجتمع.

حظر المستخدم:

- يمنع حظر المسؤول لنفسه.
- يحول المستخدم إلى `suspended`.
- يزيد `auth_version` لإبطال Access Tokens الحالية.
- يرفض المناقشات المعلقة ويعيد حجوزاتها.
- يخفي المناقشات المنشورة.
- يغلق البلاغات المفتوحة المرتبطة.
- يسجل كل ذلك داخل Audit Events.

إلغاء الحظر يعيد الحساب إلى `active` ويسجل عملية الإدارة، لكنه لا يعيد نشر المناقشات المخفية تلقائيًا.

---

## 10. الاستئنافات

- يسمح باستئناف المناقشة المرفوضة أو المخفية فقط.
- لا يسمح بأكثر من استئناف واحد لنفس المناقشة.
- تكرار نفس الاستئناف يعيد النتيجة idempotently.
- لا يمكن قبول استئناف مستخدم موقوف.
- قبول استئناف مناقشة مرفوضة يحتاج توقعًا منظمًا ورصيدًا قدره `50` نقطة وقت النشر.
- قبول استئناف مناقشة مخفية يعيدها من دون خصم إضافي.
- رفض الاستئناف يحتاج reason code.
- جميع القرارات تسجل في:
  - `discussion_moderation_events`
  - `community_admin_events`

---

## 11. الاختبارات المنفذة

ملفات الاختبارات الحالية:

```text
backend/tests/test_community_service.py
backend/tests/test_community_api.py
backend/tests/test_community_ai.py
backend/tests/test_community_safety.py
backend/tests/test_community_admin.py
```

تشمل التغطية:

- حجز، تأكيد، وتحرير الرصيد.
- منع الخصم المكرر.
- قبول ورفض القواعد الثابتة.
- قبول ورفض AI.
- فشل مزود AI وبقاء الحجز.
- استخراج توقع غير واضح.
- المجتمع، مناقشاتي، البلاغ، الكتم وإلغاء الكتم.
- Rate limits وبصمة التكرار.
- موافقات ورفض وإخفاء واستعادة الإدارة.
- حظر وإلغاء حظر المستخدم.
- Audit events.
- إنشاء وحسم الاستئنافات.
- الخصم الآمن عند قبول استئناف مناقشة مرفوضة.
- دورة Alembic الكاملة على PostgreSQL.

---

## 12. حالة CI الموثقة

آخر تشغيل موثق على Head:

```text
Workflow run: 30178050549
Head commit: d790acc9089a7d34a0e9f9e1644c4808de520be1
Conclusion: success
```

الوظائف الناجحة:

- Repository lint.
- Core compile/Ruff/tests.
- Backend compile/Ruff/tests.
- PostgreSQL test database.
- Alembic upgrade → downgrade base → upgrade.
- Flutter formatting.
- Flutter analyze.
- Flutter tests.
- Android debug APK build.

تم أيضًا تحسين CI ليجلب revision المطلوب بدقة ويشغل قاعدة اختبار PostgreSQL من سكربتات الريبو.

---

## 13. حالة PR الحالية

```text
PR: #15
State: open
Draft: true
Mergeable: true
Branch: agent/phase-6-community-core
Base: main
```

وقت كتابة السجل كان PR يحتوي تقريبًا على:

- 48 commits
- 26 changed files
- 4,985 additions
- 26 deletions

لا يتم دمجه الآن لأن Backend المرحلة أصبح قويًا، لكن تجربة Flutter للمجتمع لم تُنفذ بعد.

---

## 14. المتبقي قبل إعلان المرحلة 6 مكتملة

الأولوية التالية بالترتيب:

1. إضافة Models وRepositories الخاصة بالمجتمع داخل Flutter.
2. إنشاء صفحة المجتمع وFeed مع الفلترة والتحديث والحالات الفارغة والأخطاء.
3. إنشاء نموذج كتابة مناقشة:
   - اختيار السهم
   - العنوان
   - المحتوى
   - الفترة
   - توضيح حجز `0.5` عملة
   - منع الضغط المكرر عبر submission key ثابت للمحاولة
4. إنشاء صفحة تفاصيل المناقشة.
5. إضافة الإبلاغ والكتم وإلغاء الكتم.
6. إنشاء صفحة مناقشاتي وعرض:
   - قيد المراجعة
   - منشورة
   - مرفوضة والسبب
   - مخفية
   - حالة الاستئناف
7. إنشاء واجهة تقديم الاستئناف وعرض حالته.
8. ربط تحديث الرصيد بعد النشر أو الرفض أو قبول الاستئناف.
9. إضافة اختبارات Flutter للـmodels والـrepositories والـwidgets والتدفقات الحساسة.
10. تحديث التوثيق وROADMAP بعد اكتمال Flutter فقط.
11. تشغيل CI نهائي كامل على آخر commit.
12. تحويل PR من Draft إلى Ready ثم دمجه بعد المراجعة.
13. بعد الدمج فقط يبدأ العمل في المرحلة 7 — تقييم التوقعات.

---

## 15. قيود وتحذيرات لا يجب نسيانها

- لا يتم الوثوق برصيد أو قرار مراجعة قادم من Flutter.
- لا يتم تعيين مكافأة أو خصم بواسطة AI.
- لا يتم نشر المحتوى عند فشل AI تلقائيًا.
- لا يتم وضع مفاتيح Groq أو Open-WebUI في Flutter أو Git.
- اختبارات AI الحالية تستخدم خدمات Fake/controlled داخل الاختبارات؛ يلزم اختبار تكامل آمن مع مزود فعلي قبل الإنتاج.
- Admin access الحالي يعتمد على `ADMIN_EMAILS` ويحتاج إعدادًا صحيحًا في البيئة.
- مرحلة 5 مكتملة برمجيًا فقط؛ ما زال شراء Google Play الحقيقي وAdMob SSV الحي اختبارين إلزاميين قبل الإطلاق.
- لا يتم تحديث ROADMAP إلى أن المرحلة 6 مكتملة ومدمجة فعليًا.

---

## 16. نقطة الاستكمال الموصى بها

ابدأ من Flutter، ولا تعد كتابة Backend المجتمع من الصفر.

الفرع الصحيح:

```text
agent/phase-6-community-core
```

الخطوة الأولى في الجلسة التالية:

```text
راجع بنية mobile الحالية، ثم أضف Community API models/repository مع اختبارات parsing وnetwork errors، وبعدها ابنِ صفحة Feed قبل نموذج الإنشاء.
```

لا يتم الدمج أو الانتقال للمرحلة 7 قبل نجاح جميع اختبارات Flutter وAndroid APK على آخر Head.
